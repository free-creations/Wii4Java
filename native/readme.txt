To compile this project you need the cwiid library.
  sudo apt-get install libcwiid-dev
To get the code assistance to work correctly in NetBeans, do the following:
Open Tools/Options/C++/Code Assistance. Add Include paths to the JNI includes.